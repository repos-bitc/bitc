// The interesting part of this is in the makefile, which adds
// libbitc.bita to the command line so that the compiler will be
// forced to ingest that. The purpose of the test is to ensure that
// libbitc.bita is well-formed and parseable.
bitc version 0.11
(import bitc.main as main)
(provide bitc.main main)

(define main.main:(fn (vector string) -> int32)
  (lambda (argv)
    0))
