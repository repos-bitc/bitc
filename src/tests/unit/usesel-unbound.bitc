bitc version 0.11
import bitc.main as main
import bitc.stdio // as stdio
import bitc.list // as lst
provide bitc.main main

def main.main(argv : string[])
in
  let x = cons("using lst.* and stdio.* when importing list and stdio to global scope\n", nil)
  in
    lst.map(lambda(s) { stdio.write_string(stdio.stdout, foo.car(cons(s, mil))) },
            x)
  0:int32
