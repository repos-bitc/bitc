(define df
  (let ((f (lambda (x) x)))
    (let ((g (lambda (x) x)))
      (set! g f)
      (f #t)
      (f #\c) )))


;(provide main bitc.main)
;(define main.main:(fn ((vector string)) int32) 
;  (lambda (argvec) 0:int32)) 

;; Cyclic type
;;(define f (lambda (x) f))

