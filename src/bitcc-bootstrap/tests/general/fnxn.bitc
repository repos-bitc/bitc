;(define (id x) x)

;(define (id2 x) (id x) ())

;define x (set! id id))

;(provide main bitc.main)
;(define main.main:(fn ((vector string)) int32) 
;  (lambda (argvec) 0:int32)) 

;; Cyclic type
;;(define f (lambda (x) f))

(deftypeclass (Eq 'a)
  == : (fn ('a) bool)  )

(deftypeclass (forall ((Eq 'a)) (Ord 'a))
  <= : (fn ('a) bool)  )
  

(define h (lambda (x) #t))

(definstance (Eq bool) h)
(definstance (Ord bool) h)

(define g (<= #t)  )
