(define (id x) x)

;(define (P x) (+ x 1))

(provide main bitc.main)
;(define main.main:(fn ((vector string)) int32)
;  (lambda (argVec) (+ 1 0)))

(define (main.main:(fn ((vector string)) int32) argvec)  0)
